{
  "name": "pantry_backend",
  "version": "1.0.0",
  "description": "backend express server for pantrytopan.org aka recipe-finder",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf ./build && npx tsc",
    "start": "npm run build && node -r tsconfig-paths/register build/index.js",
    "instance": "node src/index.js"
  },
  "author": "Airor",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "@types/node": "^16.4.1",
    "nodemon": "^2.0.12",
    "ts-node": "^10.1.0",
    "tsconfig-paths": "^3.10.1",
    "typescript": "^4.3.5"
  }
}
